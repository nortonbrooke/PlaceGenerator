import React from 'react'
import classnames from 'classnames'

const PlaceLoading = () => {
  return (<div className={classnames('App-place', 'back', 'loading', 'shake')} />)
}

export default PlaceLoading
